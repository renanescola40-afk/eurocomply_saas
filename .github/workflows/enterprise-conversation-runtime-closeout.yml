name: Enterprise Conversation Runtime Closeout (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Refuse deprecated manual evidence fan-in
    runs-on: ubuntu-24.04
    steps:
      - name: Explain canonical replacement and fail closed
        shell: bash
        run: |
          echo 'This workflow is intentionally deprecated because user-supplied run IDs and first-JSON selection are not an acceptable Enterprise evidence authority.' >&2
          echo 'Use Enterprise 100 Final Authority, which discovers exact-SHA producer artifacts by hard-coded workflow path and artifact name.' >&2
          exit 1
