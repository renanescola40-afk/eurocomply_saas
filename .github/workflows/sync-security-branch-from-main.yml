name: Sync Security Branch From Main

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled temporary workflow
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "Temporary branch sync workflow is disabled and retained only so it can be removed after the branch is synced with main."
