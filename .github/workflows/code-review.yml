name: Code Review
on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: read
  pull-requests: write

jobs:
  setup-deepseek-review:
    runs-on: ubuntu-latest
    name: Code Review
    env:
      DEEPSEEK_REVIEW_CREDENTIAL: ${{ secrets.CHAT_TOKEN }}
    steps:
      - name: Skip DeepSeek code review when CHAT_TOKEN is not configured
        if: env.DEEPSEEK_REVIEW_CREDENTIAL == ''
        run: echo "CHAT_TOKEN is not configured; skipping DeepSeek Code Review."

      - name: DeepSeek Code Review
        if: env.DEEPSEEK_REVIEW_CREDENTIAL != ''
        uses: hustcer/deepseek-review@v1
        with:
          chat-token: ${{ env.DEEPSEEK_REVIEW_CREDENTIAL }}
