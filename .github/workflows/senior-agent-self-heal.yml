name: Senior Agent Manual Repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-repair:
    name: Manual repair placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Explain status
        run: echo "Senior Agent manual repair workflow is a non-mutating placeholder until provider setup is complete."
