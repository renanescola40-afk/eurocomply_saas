# Apply instruction

Append the `entry` object from `agent_log_patch_2026_06_18.json` to the `entries` array in `agent_log.json` before or during merge review.

This is a prepared non-runtime audit-log update only.
