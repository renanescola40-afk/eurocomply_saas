# P1-06 artifact attestation evidence

Workflow run `27779627063` completed the job `Generate SBOM and attest provenance` successfully.

The following step completed successfully:

- Step: `Attest SBOM artifact provenance`
- Action: `actions/attest-build-provenance@v3`
- Subject path: `sbom.cdx.json`

The successful step is recorded in the GitHub Actions job `82200790960` for run `27779627063`.

This record is referenced by `docs/security/evidence/p1/sbom-artifact-attestation.json`.
