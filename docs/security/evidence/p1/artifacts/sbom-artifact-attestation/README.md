# P1-06 SBOM artifact attestation evidence artifacts

This directory contains durable text references for the reviewed GitHub Actions evidence behind `docs/security/evidence/p1/sbom-artifact-attestation.json`.

The source evidence was the successful `P1 SBOM and Artifact Attestation` workflow run `27779627063`, including the `p1-sbom-cyclonedx` artifact and successful SBOM provenance attestation step.

No secrets are stored in this directory.
