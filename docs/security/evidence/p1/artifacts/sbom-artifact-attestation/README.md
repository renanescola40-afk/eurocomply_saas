# P1-06 SBOM + artifact attestation artifacts

This directory contains durable evidence notes for P1-06 final evidence.

The source evidence is GitHub Actions run `27779627063`, artifact `p1-sbom-cyclonedx`, and the successful artifact provenance attestation job step.

The final evidence file is:

`docs/security/evidence/p1/sbom-artifact-attestation.json`
