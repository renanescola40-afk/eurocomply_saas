# Provider Runtime Proof — Supabase/Auth Front — 2026-08-21

**Status:** `HUMAN_BLOCKER`  
**Evidence capture date:** `2026-08-21`  
**Repository:** `renanescola40-afk/eurocomply_saas`  
**Runtime evidence subject SHA:** `7c063edbd73e719024666b7740623455aae20f0d`  
**Production deployment:** `dpl_rbbHqrqcXWfAchSZs46ZeLoYZETw`  
**Production URL:** `https://www.risckcomply.com`

This evidence is scoped only to providers directly required by the Supabase/Auth production-closure front. Stripe/billing, independent pentest and Legal evidence remain outside this stream.

No API keys, OAuth client secrets, cookies, bearer tokens or customer identifiers are retained below.

## Provider inventory

| Provider | Purpose | Production config evidence | Server-side secret boundary | Real runtime call/evidence | Mock disabled / non-crediting fallback | Correlation evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Supabase Postgres / RLS | production tenant data and authorization | connected project `tganhbbhfxcpblmgqprg`, `ACTIVE_HEALTHY`, `eu-west-1`; production CSP references same project | service-role construction is `server-only`; browser uses public key only | direct production SQL/catalog and negative RLS tests executed against the connected project | live DB, not fixture/mock | production HTTP probes carry `x-request-id`; DB proof is tied to exact project ref and capture date | `PASS` for project/runtime identity + live RLS behavior; overall closure blocked by schema promotion |
| Supabase Auth | primary application identity/session authority | production project has active public client authority; repository runtime uses Supabase Auth SSR/browser clients | service role is not used as end-user session authority | production protected-route/callback probes executed; real production Auth identity records exist | live production Auth, not mock | callback/readiness probes include request IDs; production runtime logs captured callback requests | `PARTIAL` |
| Google OAuth via Supabase Auth | release-critical social login provider | real `google` identities exist in the production Supabase Auth project; last attributable successful sign-in observed `2026-08-15T13:50:36.786687Z` | Google client secret is expected only in provider/server secret stores; repository does not expose it as `NEXT_PUBLIC_` | historical real Google sign-in proven; fresh exact-release end-to-end OAuth consent/callback was not executable without an authorized test identity/browser session | no unit/mock evidence is credited as runtime proof | live callback probes have request IDs; fresh Google request/provider correlation ID still required | `HUMAN_BLOCKER` |

## Supabase production runtime proof

### Production identity binding

The canonical production HTTP surface and the connected Supabase control plane converge on the same project:

- production CSP includes `https://tganhbbhfxcpblmgqprg.supabase.co`;
- connected project ref is `tganhbbhfxcpblmgqprg`;
- project state is `ACTIVE_HEALTHY`, region `eu-west-1`;
- active public client keys are present for this same project; exact values are omitted;
- production HTML exposes `sentry-release=7c063edbd73e719024666b7740623455aae20f0d`, binding the observed app runtime to the evidence subject SHA.

### Real live calls

The following are non-mocked production observations:

1. `GET https://www.risckcomply.com/api/health` → HTTP `200`, `status=ok`, request ID `req_9f936e5c-f7c2-4d7a-bbe7-cbac2f30b7b3`.
2. Anonymous `GET https://www.risckcomply.com/pt/dashboard` → login surface, no private dashboard payload.
3. `GET https://www.risckcomply.com/api/ready` without protected health authorization → HTTP `401`, correlated request ID; security-critical readiness is not anonymously disclosed.
4. `GET https://www.risckcomply.com/auth/callback?locale=pt` without OAuth code → HTTP `307` through localized callback handling and then login with `missing_oauth_code`; no session created.
5. Production runtime logs for the exact deployment recorded the `/auth/callback` and localized callback requests generated by the probe. Runtime error aggregation found no selected-route errors in the inspected window.
6. Direct production PostgreSQL/RLS calls were executed through the connected Supabase project and returned the tenant-isolation results documented in `supabase-production-closure-2026-08-21.md`.

## Google OAuth evidence boundary

Production database evidence confirms that Google OAuth has completed successfully for real users in this Supabase project in the past: `google` identities exist and the latest observed successful sign-in is `2026-08-15T13:50:36.786687Z`.

That is not sufficient for `PROVIDER_RUNTIME_PROOF: PASS` on runtime subject `7c063edbd73e719024666b7740623455aae20f0d`. A fresh exact-release proof must establish the whole chain:

`production login → Supabase OAuth authorize → Google consent/provider → production callback → code exchange → authenticated protected route → logout → protected route denied`

A unit test, a provider screenshot, an environment-variable listing, the historical identity row or a callback request without a real code is not substituted for that proof.

### HUMAN BLOCKER — fresh Google provider runtime proof

**System:** Google OAuth + Supabase Auth + RISCK COMPLY production  
**Reason:** Completing Google consent and returning a valid one-time authorization code requires an authorized end-user Google identity/browser session. No designated production test credential/session is available to the agent, and credentials must not be fabricated, extracted or logged.  
**Exact human action:** Use a designated non-privileged production test identity at `https://www.risckcomply.com/pt/login`; start Google sign-in; complete consent; verify the real callback completes; capture UTC time plus non-secret request/correlation/provider request IDs where exposed; verify the intended localized onboarding/dashboard route; logout; then verify the protected route is denied/redirected to login.  
**Expected confirmation:** Real Google request succeeds without mock/fallback; callback exchanges the code; authenticated route works only during the session; logout invalidates access; no secret appears in browser-visible logs/evidence.  
**Technical work that can continue meanwhile:** Supabase project identity, RLS, schema drift, server-only service-role boundary, callback fail-closed behavior, runtime request correlation and migration handoff are already captured.

## Failure-mode observations

| Failure mode | Observed / reviewed behavior | State |
| --- | --- | --- |
| anonymous protected route | redirects/resolves to login; no private dashboard content | `PASS` |
| missing/invalid callback code | redirect to login with explicit auth error; no session | `PASS` |
| missing readiness authorization | HTTP `401` with request ID | `PASS` |
| Supabase public config missing in middleware | treated as unauthenticated | `PASS` by code boundary; destructive production mutation not performed |
| Supabase `getUser()` error | treated as unauthenticated | `PASS` by code boundary |
| OAuth code exchange failure | login redirect, failed session not accepted | `PASS` by code boundary |
| session user unavailable after exchange | sign-out attempt + login redirect | `PASS` by code boundary |
| fresh Google provider 401/429/500/timeout | not safely inducible against production without sabotaging a real provider/session | `HUMAN_BLOCKER` for exact runtime failure injection; code must remain fail-closed |
| provider secret absent | production secret was not removed to manufacture evidence | `NOT_APPLICABLE` as a live destructive test; configuration absence must never be simulated by changing production |

## Observability / secret handling

- Production probes emitted `x-request-id` values.
- Current callback runtime logs do not contain OAuth codes or credentials in the retained evidence.
- Repository callbacks log symbolic error events such as `auth_exchange_failed` rather than raw provider secret material.
- Health/readiness responses do not expose Supabase keys or service-role credentials.
- Exact public/client key values returned by provider control-plane tooling were intentionally omitted from this file.

## Acceptance result

`PROVIDER_RUNTIME_PROOF: HUMAN_BLOCKER`

PASS is prohibited until the fresh exact-release Google OAuth request/callback/session/logout proof exists. Supabase project identity and live RLS behavior are proven; historical Google sign-in evidence is retained as supporting, non-crediting evidence for the current-release PASS decision.

The documentation-containing commit SHA may differ from the runtime evidence subject SHA. This file does not claim its own commit is deployed.
