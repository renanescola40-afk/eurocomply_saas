# Supabase Live RLS Plan Template

This folder contains the plan template used before collecting final Supabase live RLS runtime evidence.

The plan template is safe to keep in the repository because it contains no production values. Runtime values and redacted execution output must be handled outside the repository until they are converted into the final reviewed runtime evidence JSON.
