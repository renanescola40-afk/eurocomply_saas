# P1 All Evidence Builder

This helper runs every P1 evidence builder in sequence.

Run:

```bash
node scripts/security/build-p1-all-evidence.mjs
```

Each individual builder still requires its reviewed local input file. This helper does not create proof by itself and does not mark any control complete.
