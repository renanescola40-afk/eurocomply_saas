# P1 DAST main trigger review note

This branch adds only a workflow trigger follow-up for P1-05 DAST automation.

Review points:

- It adds a `push` trigger for `main` scoped to the DAST workflow and DAST automation documentation paths.
- It does not add secrets or production credentials.
- It does not change P1 evidence status.
- P1-05 remains `Open` until a successful DAST run and report artifact are reviewed and formalized.
