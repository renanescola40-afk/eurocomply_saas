#!/usr/bin/env node

// Canonical wrapper for the public production release runner.
// CI validates the full command contract across this file and the v2 runner.

import './run-public-production-release-v2.mjs';
