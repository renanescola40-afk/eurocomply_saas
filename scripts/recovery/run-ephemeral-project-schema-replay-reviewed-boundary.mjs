#!/usr/bin/env node

// Compatibility entrypoint retained for stale references. The canonical
// reviewed-boundary implementation is the syntax-safe v2 module.
import './run-ephemeral-project-schema-replay-reviewed-boundary-v2.mjs';
